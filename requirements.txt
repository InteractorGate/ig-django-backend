asgiref==3.11.1
Django==5.1.15
django-cors-headers==4.9.0
django-environ==0.13.0
djangorestframework==3.15.2
djangorestframework_simplejwt==5.5.1
dnspython==2.8.0
gunicorn==26.0.0
mssql-django==1.7.2
packaging==26.2
PyJWT==2.13.0
pymongo==4.17.0
pyodbc==5.3.0
pytz==2026.2
sqlparse==0.5.5
tzdata==2026.2
whitenoise==6.12.0

# ── Deep Learning: RNN contextual text prediction (Phase 5) ──────────────────
# The Docker image installs the CPU build of torch (see Dockerfile) to avoid the
# multi-GB CUDA wheels — Azure App Service B1 has no GPU. "==2.12.1" matches the
# "2.12.1+cpu" local build, so this pin is satisfied by the CPU wheel.
numpy==2.5.0
torch==2.13.0

# ── CNN gaze estimation (Phase 5) ────────────────────────────────────────────
# Inference needs only torch+numpy (the trained artifact is committed). scipy is
# required ONLY to (re)train from the MPIIGaze .mat files — install it manually
# for training; it is intentionally NOT a runtime/deploy dependency.
#   pip install scipy   # then: python -m ai_modules.cnn.train
